<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>V2L & No Separator</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f0f2f5;
        }
        h1 {
            text-align: center;
        }
        .container {
            display: flex;
            justify-content: space-around;
            margin-top: 30px;
        }
        .column {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            width: 40%;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        ul {
            list-style: none;
            padding-left: 0;
        }
        li {
            background: #e2e8f0;
            margin: 5px 0;
            padding: 8px;
            border-radius: 4px;
        }
        textarea {
            width: 100%;
            height: 100px;
            margin-bottom: 10px;
            padding: 10px;
            border-radius: 4px;
            border: 1px solid #ccc;
            resize: vertical;
        }
        button {
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            background-color: #4CAF50;
            color: white;
            cursor: pointer;
        }
        button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>

    <h1>V2L & No Separator</h1>

    <textarea id="inputList" placeholder="Enter items separated by commas (e.g., v2l1, no1, v2l2)"></textarea>
    <button onclick="separateItems()">Separate</button>

    <div class="container">
        <div class="column">
            <h2>V2L</h2>
            <ul id="v2lList"></ul>
        </div>
        <div class="column">
            <h2>No</h2>
            <ul id="noList"></ul>
        </div>
    </div>

    <script>
        // Example pre-filled items
        const exampleItems = ["v2l1", "no1", "v2l2", "no2", "v2l3"];
        document.getElementById("inputList").value = exampleItems.join(", ");

        function separateItems() {
            const input = document.getElementById("inputList").value;
            const items = input.split(",").map(item => item.trim());
            
            const v2l = items.filter(item => item.toLowerCase().startsWith("v2l"));
            const no = items.filter(item => item.toLowerCase().startsWith("no"));

            const v2lList = document.getElementById("v2lList");
            const noList = document.getElementById("noList");

            v2lList.innerHTML = "";
            noList.innerHTML = "";

            v2l.forEach(item => {
                const li = document.createElement("li");
                li.textContent = item;
                v2lList.appendChild(li);
            });

            no.forEach(item => {
                const li = document.createElement("li");
                li.textContent = item;
                noList.appendChild(li);
            });
        }

        // Automatically separate on page load
        separateItems();
    </script>

</body>
</html>
